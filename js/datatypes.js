// types of Datatypes

// 1. premitive      // rom - fixed
// 2. non-premitive  // rom - flexible


// 1 bit

// 8bit 1bite

// 32bite  always extra there

// 1032bites 1kb // kilobites

// 1000kb  1mb 
// 1000mb 1gb
// 1000gb 1tb

// for premitive datatypes memory allocation to variables is fixed
// boolean datatype 1byte
// number datatype  4byte / 8byte
// string datatype  32byte
// char datatype 1byte
// etc


// for non-premitive datatypes memory allocation is not fixed. it is flexible.
// types of non-premitive datatypes 
// 1. arrays  2. objects  3. functions 



let arr = [1, 3, 5, 7, 9, 11, 13];
arr = "sdfefsefse";
arr = [2, 4, 6, 8, 10];
arr = {
    numbers: [2, 5, 8],
    strings: ['apple', 'zain', 'guava'],
    bool : false,
    
}

console.log(arr);
arr.push(15);
console.log(arr);
arr.pop();
arr.pop();
console.log(arr);